export enum InteractEvent
 {
    DELETE,
    DISABLE,
    CANCEL,
    CONFIRM_ENABLE,
    CONFIRM_DISABLE,
    INTERACT_CALL,
}

export enum InteractParticipant
{
   SOMEONE,
   ALL,
   FEW,
}

export enum InteractionName
{
   STARTUP,
   VOTE,
}
